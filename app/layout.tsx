// file: app/layout.tsx
import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

// Fonts
const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

// Metadata global untuk SEO & aksesibilitas
export const metadata: Metadata = {
  title: "Sistem Pendaftaran Kelas",
  description: "Aplikasi web untuk pendaftaran kelas dengan kuota real-time. Siswa dapat mendaftar kelas, melihat sisa kuota, dan admin dapat mengelola kelas.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="id">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
